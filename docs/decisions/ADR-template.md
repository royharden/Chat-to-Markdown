# ADR-NNNN: <title>

**Status:** Proposed | Accepted | Superseded by ADR-XXXX
**Date:** YYYY-MM-DD

## Context
What forces are at play; the problem or decision on the table.

## Decision
As a Y-statement: *In the context of `<use case>`, facing `<concern>`, we decided `<option>` to achieve `<quality>`, accepting `<downside>`.*

## Consequences
What becomes easier or harder. Any floors changed (cite `floor.json`). Follow-ups.

---
*When to write an ADR:* any durable choice that future agents should not relitigate — a bundler/tooling decision, an architectural boundary, lowering a `floor.json` floor, or changing an invariant in `ARCHITECTURE.md`. Number sequentially (ADR-0001, ADR-0002, …).
