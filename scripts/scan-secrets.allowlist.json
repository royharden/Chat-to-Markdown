{
  "note": "Justified exceptions for scripts/scan-secrets.mjs. Every entry needs a known rule, a repo-relative path (glob: * within a folder, ** across folders), a non-empty reason, and optionally the exact matched text. Prefer fixing the file to adding an entry.",
  "allow": []
}
