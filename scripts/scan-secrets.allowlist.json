{
  "note": "Policy for scripts/scan-secrets.mjs. `allow`: justified exceptions; every entry needs a known rule, a repo-relative path (glob: * within a folder, ** across folders), a non-empty reason, and optionally the exact matched text. Prefer fixing the file to adding an entry. `denyInFixtures`: names that must never appear inside test/fixtures (account menus and avatars print them).",
  "denyInFixtures": ["Roy Harden", "royharden"],
  "allow": []
}
